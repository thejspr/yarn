Given /^a client "(.*)"$/ do |client|
  pending # express the regexp above with the code you wish you had
end

When /^client "(\S*)" makes a "(\S*)" request$/ do |client,speed|
  pending # express the regexp above with the code you wish you had
end

Then /^client B should receive a response$/ do
  pending # express the regexp above with the code you wish you had
end
